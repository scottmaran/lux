This directory holds one Remotion project per video.

- Existing projects may be legacy references.
- New projects should be created via:
  - `npm run new-video -- --name <project-name>`
- Shared skills are managed at:
  - `../skills/`
