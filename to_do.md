# To Do
- add support for openclaw
- add ```lux info``` CLI command for more thorough documentation
- remove dependency on Docker by using native containers for Linux & MacOS
- refine filtered timeline so logging UI gives better human-readable output
- add lux skill